name: Releases

on:
  push:
    branches: 
      - main

jobs:

  build:
    runs-on: windows-latest

    steps:

      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Run console
        run: npm i && npm run create
        shell: bash 

    